a = DivStr()
print(* a // 4, a % 4, a.upper(), sep='#')
a = DivStr('0123456789')
b = DivStr(a.isalpha())
print(* a // 2, * b // 3, b % 3)
a = DivStr(2**32)
print(sum(int(i) for i in a // 5))
b = DivStr(a[::-1] + b)
print(*(int(str(s), base=16) for s in b[3:-4] // 4), sep='.')
